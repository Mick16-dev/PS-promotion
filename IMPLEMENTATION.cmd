@echo off
REM IMPLEMENTATION PLAN FOR ROHR-BLITZ PLUMBING WEBSITE
REM ================================================

REM 1. VISUAL CLEANUP
REM - Remove headline underlines in hero, features, and gallery sections.
REM - Remove overly strong mesh / grain backgrounds where they distract from content.
REM - Remove decorative certification seal image that does not add value.
REM - Tone down hover effects on icons and logo to feel calmer and more professional.
REM - Neutralize the magnetic cursor-follow effect on buttons.

REM 2. BEFORE/AFTER GALLERY
REM - Keep the comparison slider but remove hover zoom to avoid gimmicky feel.
REM - Ensure the "after" image displays clearly behind the clipped "before" image.

REM 3. TEAM SECTION
REM - Reposition the “Master Expert” section to focus on the people (team) instead of abstract authority.
REM - Update copy to describe who visits the customer, how they work, and what to expect on-site.
REM - Keep the existing expert profile cards but adjust bullet points to be more homeowner-friendly.

REM 4. LEARN MORE PAGE
REM - Create a dedicated /team page using the existing header, team section, and footer.
REM - Reuse the updated team section so content stays consistent between homepage and team page.
REM - Keep emergency and CTA hooks minimal on this page so focus stays on the team.

REM 5. LEARN MORE ENTRY POINT
REM - Wire the “Learn more” text in the features section to link to /team.
REM - Ensure link styling is subtle (no heavy animations) and readable.

REM 6. QA CHECKLIST
REM - Run `npm run lint` and `npm run build` to ensure the app compiles cleanly.
REM - Test homepage and /team on desktop and mobile viewports.
REM - Verify that buttons still work normally after magnetic effect is neutralized.
REM - Confirm copy is readable against backgrounds in light and dark modes (if applicable).

echo Implementation plan loaded. Review this file before making future design changes.
pause

